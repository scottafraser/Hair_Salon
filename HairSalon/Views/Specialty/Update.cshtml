@{
    ViewData["Title"] = "Client Update";
}

<form action="/stylist/@Model.GetId()/update" method="post">
    <div class='form-group'>
        <label for='newName'>New Stylist Name</label>
        <input id='newNam' name='client' class='form-control' type='text' required>
    </div>
    <button class="btn btn-primary" type="submit"> Update Client</button>
    </form>
